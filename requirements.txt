numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
joblib>=1.3
shap>=0.44
pytest>=7.4

# Optional — ONNX export (src/export_edge_model.py falls back to joblib-only
# if these are not installed):
onnx>=1.14
skl2onnx>=1.16
